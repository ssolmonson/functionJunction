var moveZeros = function (arr) {
  // 
  // let newarray
  // filter array for 0
  //
  // run through array remove 0's
  //
  // after loop through the original array
  //
  // concat removedarray with newarray
}
